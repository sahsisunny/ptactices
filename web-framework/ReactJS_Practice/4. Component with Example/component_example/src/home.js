import React from 'react';

export default class Home extends React.Component {
     render() {
          return (
               <div>
                    <h1>Home</h1>
                    <p>Home page body content</p>
               </div>
          );
     }
}

// render() is used to render the component on the screen and return the HTML code to be rendered.