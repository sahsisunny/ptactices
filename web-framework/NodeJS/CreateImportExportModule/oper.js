const add = (a, b) => {
     return a + b;
};

const sub = (a, b) => {
     return a - b;
};

const mult = (a, b) => {
     return a * b;
};

const div = (a, b) => {
     return a / b;
};

const name = "Sunny Sahsi";

module.exports = {
     add,
     sub,
     mult,
     div,
     name
};        // Exporting the module as an object